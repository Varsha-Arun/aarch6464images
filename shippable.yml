language: java

# Version number
java:
  - openjdk7
  - openjdk8
  - oraclejdk8

runtime:
  nodePool: custom__aarch64__Ubuntu_16_04

build:
  pre_ci_boot:
    image_name: drydockaarch64/u16javall
    image_tag: master
    
  ci:
    - java -verison
    - docker ps
   # - docker build -t=test .
    - docker images
   # - mvn --version    
   # - ant -version 
   # - gradle --version
    